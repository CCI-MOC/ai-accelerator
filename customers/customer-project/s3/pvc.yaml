apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: rustfs-data-block
  namespace: cust-project
spec:
  storageClassName: local-storageclass
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
  volumeMode: Filesystem
