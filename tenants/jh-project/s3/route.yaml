apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: rustfs-s3
  namespace: jh-project-shared
spec:
  to:
    kind: Service
    name: rustfs-s3
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: rustfs-console
  namespace: jh-project-shared
spec:
  to:
    kind: Service
    name: rustfs-console
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
