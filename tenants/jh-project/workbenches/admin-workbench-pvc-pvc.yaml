apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: admin-workbench-pvc
  namespace: jh-project
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  volumeMode: Filesystem
